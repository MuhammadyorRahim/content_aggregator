# ──────────────────────────────────────────────
# Content Aggregator — Production Environment
# Copy to /app/.env.local on the server
# ──────────────────────────────────────────────

# Database (SQLite — default path)
DATABASE_URL="file:./data.db"

# Auth.js — REQUIRED (generate with: openssl rand -base64 32)
AUTH_SECRET="CHANGE_ME_GENERATE_A_NEW_SECRET"
AUTH_TRUST_HOST=true
AUTH_URL="http://YOUR_SERVER_IP"

# Registration mode: open | invite-only | closed
REGISTRATION_MODE=open

# RSSHub (local Docker container)
RSSHUB_BASE_URL="http://localhost:1200"

# Optional: Google OAuth (leave empty to disable)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# Optional: Stripe billing (leave empty to disable)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# STRIPE_PRO_PRICE_ID=
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Optional: Resend email (leave empty to disable)
# RESEND_API_KEY=
# RESEND_FROM_EMAIL=noreply@yourdomain.com

# Optional: YouTube API
# YOUTUBE_API_KEY=

# Optional: Worker tuning
# CACHE_DURATION_PRO_MINUTES=15
# CACHE_DURATION_FREE_MINUTES=30
